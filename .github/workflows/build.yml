# AUDIT FIX (P0.1): continuous build so firmware regressions are caught in CI.
name: PlatformIO Build

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  # Native (host) tests for the General-Midi-Boop v2 capability layer. No ESP32
  # toolchain needed, so regressions surface in seconds instead of after a full
  # firmware build.
  native-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run native GMB tests
        run: make -C test

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Cache PlatformIO
        uses: actions/cache@v4
        with:
          path: |
            ~/.platformio
            ~/.cache/pip
          key: ${{ runner.os }}-pio-${{ hashFiles('platformio.ini') }}
          restore-keys: ${{ runner.os }}-pio-

      - name: Install PlatformIO
        run: |
          python -m pip install --upgrade pip
          pip install platformio

      - name: Build firmware
        run: pio run
